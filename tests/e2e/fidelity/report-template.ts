import type { ReportData } from './report';

/** No network dependencies: the report opens directly from disk. */
export function renderReport(data: ReportData): string {
  const json = JSON.stringify(data).replace(/</g, '\\u003c');
  return `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Turbo Pascal · Reference comparison</title>
<style>
:root{color-scheme:dark;font-family:ui-sans-serif,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;background:#10131b;color:#e2e7f2}*{box-sizing:border-box}body{margin:0}a{color:#93b9ff}button,input,select{font:inherit}button,select{background:#222b3b;color:#eef3ff;border:1px solid #435069;border-radius:6px;padding:7px 10px;cursor:pointer}button:hover{background:#334260}header{padding:24px 30px 16px;border-bottom:1px solid #2b3547;background:#171c28}header p{color:#aebbd0;max-width:1020px;font-size:14px;line-height:1.55;margin:9px 0}h1{font-size:26px;margin:0;font-weight:650;letter-spacing:-.6px}.eyebrow{font-size:11px;letter-spacing:2px;color:#7faaf9;text-transform:uppercase;margin-bottom:8px}.stats{display:flex;gap:28px;flex-wrap:wrap;margin-top:16px}.stat strong{display:block;font-size:26px;font-variant-numeric:tabular-nums}.stat span{font-size:12px;color:#a4b4cc}.warning{padding:10px 12px;background:#503514;color:#ffe0aa;border-radius:6px;margin-top:10px}.layout{display:grid;grid-template-columns:310px minmax(0,1fr);min-height:calc(100vh - 235px)}aside{background:#151a25;border-right:1px solid #2b3547;min-width:0}.filters{padding:14px;display:grid;gap:9px;position:sticky;top:0;background:#151a25;z-index:2}.filters input{width:100%;padding:10px;border:1px solid #435069;border-radius:6px;background:#0e1320;color:white}.state-list{max-height:calc(100vh - 338px);overflow:auto;padding:0 8px 12px}.state{width:100%;text-align:left;display:block;padding:11px 9px;margin:4px 0;border:1px solid transparent;border-radius:5px;background:transparent}.state.active{background:#293b5d;border-color:#638ccb}.state:hover{background:#202d44}.state-name{display:block;font-size:12px;overflow-wrap:anywhere;color:#edf1f9}.state-meta{display:flex;justify-content:space-between;margin-top:6px;color:#95a6c0;font-size:11px}.green{color:#91e4b6}.amber{color:#ffd18c}.red{color:#ff91ac}.muted{color:#91a2bd}.main{padding:22px 28px;min-width:0}.titlebar{display:flex;align-items:flex-start;justify-content:space-between;gap:16px}h2{font-size:22px;margin:0 0 6px;overflow-wrap:anywhere}.small{font-size:12px;color:#a2b1c8;line-height:1.5}.pill{border:1px solid #566887;border-radius:99px;padding:3px 9px;font-size:11px;white-space:nowrap}.note{border-left:3px solid #e6b461;background:#27251f;padding:11px 13px;font-size:13px;line-height:1.5;margin:15px 0}.metrics{display:flex;gap:12px;flex-wrap:wrap;margin:17px 0}.metric{flex:1;min-width:150px;background:#1b2231;padding:12px 14px;border:1px solid #2f3c52;border-radius:7px}.metric strong{display:block;font-size:18px;font-variant-numeric:tabular-nums}.metric span{font-size:11px;color:#aebbd0}.toolbar{display:flex;align-items:center;gap:10px;flex-wrap:wrap;margin:16px 0 10px}.toolbar .spacer{flex:1}.toolbar label{font-size:12px;color:#bac6db}.viewer{position:relative;max-width:100%;width:720px;aspect-ratio:9/5;background:#060a12;outline:1px solid #3a4964;overflow:hidden}.viewer img{width:100%;height:100%;display:block;image-rendering:pixelated;position:absolute;inset:0}.viewer .current{clip-path:inset(0 50% 0 0)}.wipe-line{position:absolute;left:50%;top:0;bottom:0;border-left:2px solid #ffdc73;pointer-events:none}.mask{position:absolute;outline:1px solid #ffe08a;background:#ffd96625;pointer-events:none}.stage-labels{display:flex;justify-content:space-between;max-width:720px;font-size:11px;color:#aebbd0;margin:7px 0}.range-wrap{max-width:720px;display:flex;gap:12px;align-items:center;font-size:12px;margin-top:12px}input[type=range]{width:100%;accent-color:#90b5ff}.side-by-side{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}.side-by-side figure{margin:0;min-width:0}.side-by-side img{width:100%;display:block;image-rendering:pixelated;border:1px solid #3a4964}.side-by-side figcaption{font-size:12px;color:#acbad0;margin:7px 0}.hidden{display:none!important}.empty{border:1px dashed #53627a;padding:25px;line-height:1.6;color:#adbed7;max-width:720px}.details{max-width:950px;margin-top:21px;border-top:1px solid #2c384d;padding-top:15px;font-size:12px;line-height:1.6;color:#aabbd3}.details summary{cursor:pointer;color:#e0e8f6}.details ul{padding-left:20px}.details code{overflow-wrap:anywhere;color:#c7d7ef}.details table{border-collapse:collapse;width:100%;margin-top:10px}.details td,.details th{padding:6px;border-bottom:1px solid #293449;text-align:left}.nav{display:flex;justify-content:space-between;margin-top:24px;padding-top:16px;border-top:1px solid #2c384d}footer{padding:15px 30px;border-top:1px solid #2b3547;color:#8296b5;font-size:12px}@media(max-width:900px){.layout{grid-template-columns:220px minmax(0,1fr)}.main{padding:18px 16px}.stats{gap:16px}}@media(max-width:650px){.layout{display:block}.state-list{max-height:200px}.filters{grid-template-columns:1fr 1fr}.main{padding:16px}.side-by-side{grid-template-columns:1fr}header{padding:18px}.stats strong{font-size:22px}}
</style></head><body>
<header><div class="eyebrow">Museum of UI · Independent visual audit</div><h1>Turbo Pascal — reference comparison</h1>
<p>Fresh browser captures against all <strong id="total"></strong> gallery screenshots. Every comparison includes the full, unmasked pixel difference. Adjusted metrics show explicitly excluded content regions separately. Missing subjects and partial substitutes remain visible in the inventory.</p>
<div class="stats" id="stats"></div><div id="source-warning" class="warning hidden"></div>
<p><a href="${data.gallery}" target="_blank" rel="noreferrer">Original reference gallery</a> · <a href="coverage.md">Coverage summary</a> · <a href="coverage.json">Full evidence (JSON)</a> · <span id="capture-meta"></span></p></header>
<div class="layout"><aside><div class="filters"><input id="search" aria-label="Filter screenshots" placeholder="Find a screenshot…"><select id="filter" aria-label="Coverage filter"><option value="all">All gallery screenshots</option><option value="different">Pixel differences</option><option value="exact">Exact, no exclusions</option><option value="partial">Partial subject mappings</option><option value="unmatched">Unmatched / capture errors</option></select></div><nav class="state-list" id="states" aria-label="Gallery screenshots"></nav></aside>
<main class="main"><div class="titlebar"><div><h2 id="title"></h2><div class="small" id="subtitle"></div></div><span id="badge" class="pill"></span></div><div id="note" class="note hidden"></div><div class="metrics" id="metrics"></div>
<div class="toolbar"><button data-view="overlay">Overlay wipe</button><button data-view="side">Side by side</button><button data-view="diff">Pixel diff</button><div class="spacer"></div><label><input id="show-masks" type="checkbox"> Show excluded regions</label><label id="adjusted-label" class="hidden"><input id="adjusted" type="checkbox"> Adjusted diff</label></div>
<div id="single"><div class="viewer" id="viewer"><img id="base" alt="Gallery reference screenshot"><img id="current" class="current" alt="Fresh current app screenshot"><div id="wipe-line" class="wipe-line"></div><div id="masks"></div></div><div class="stage-labels"><span id="left-label">Current app</span><span id="right-label">Gallery reference</span></div><div id="range-wrap" class="range-wrap"><span>Wipe</span><input id="wipe" type="range" min="0" max="100" value="50" aria-label="Overlay wipe position"></div></div>
<div id="side" class="side-by-side hidden"><figure><img id="side-ref" alt="Gallery reference screenshot"><figcaption>Gallery reference</figcaption></figure><figure><img id="side-current" alt="Fresh current app screenshot"><figcaption>Current app</figcaption></figure></div><div id="empty" class="empty hidden"></div>
<details class="details" open><summary>Comparison method, exclusions, and capture evidence</summary><div id="method"></div></details><div class="nav"><button id="previous">← Previous screenshot</button><button id="next">Next screenshot →</button></div></main></div>
<footer>Regenerate: start the app with <code>bun run dev</code>, fetch references with <code>bun run fidelity:fetch</code>, then run <code>bun run fidelity:report</code>. No stored visual snapshots are used as current images.</footer>
<script type="application/json" id="report-data">${json}</script><script>
const report=JSON.parse(document.getElementById('report-data').textContent),entries=report.entries;
const el=id=>document.getElementById(id),fmt=n=>Number(n).toLocaleString('en-US'),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const pct=(n,d)=>d?(100*n/d).toFixed(3)+'%':'—';
const captured=entries.filter(e=>e.status==='captured'),exact=captured.filter(e=>e.raw.pixels===0),equivalent=captured.filter(e=>e.equivalence==='equivalent');
el('total').textContent=entries.length;
el('stats').innerHTML=[[entries.length,'gallery screenshots'],[captured.length,'fresh subject captures'],[exact.length,'unmasked exact matches'],[captured.length-equivalent.length,'partial subject mappings'],[entries.length-captured.length,'unmatched / uncaptured']].map(([n,label])=>'<div class="stat"><strong>'+n+'</strong><span>'+label+'</span></div>').join('');
el('capture-meta').textContent=report.browser+' · 720 × 400 · '+new Date(report.generatedAt).toLocaleString();
if(report.sourceChangedDuringCapture){el('source-warning').classList.remove('hidden');el('source-warning').textContent='Source files changed while screenshots were captured. Per-state source hashes are recorded; regenerate after edits finish for a single-revision comparison.'}
let active=entries.findIndex(e=>e.state==='main-view'),view='overlay';if(active<0)active=0;
let visible=[];
function badge(e){if(e.status!=='captured')return e.status;if(e.equivalence==='partial')return 'partial subject';return e.raw.pixels===0?'exact':'differences'}
function list(){const q=el('search').value.toLowerCase(),f=el('filter').value;visible=entries.map((e,i)=>({e,i})).filter(({e})=>e.ref.toLowerCase().includes(q)||String(e.state??'').includes(q)).filter(({e})=>f==='all'||f==='different'&&e.raw?.pixels>0||f==='exact'&&e.raw?.pixels===0||f==='partial'&&e.equivalence==='partial'||f==='unmatched'&&e.status!=='captured');el('states').innerHTML=visible.map(({e,i})=>'<button class="state '+(i===active?'active':'')+'" data-index="'+i+'"><span class="state-name">'+esc(e.ref)+'</span><span class="state-meta"><span class="'+(e.status!=='captured'?'muted':e.equivalence==='partial'?'amber':e.raw.pixels===0?'green':'red')+'">'+badge(e)+'</span><span>'+(e.raw?pct(e.raw.pixels,e.raw.compared):'—')+'</span></span></button>').join('');el('states').querySelectorAll('button').forEach(b=>b.onclick=()=>{active=Number(b.dataset.index);render()})}
function render(){const e=entries[active];list();el('title').textContent=e.ref;el('subtitle').innerHTML='Gallery '+e.order+' / '+entries.length+(e.state?' · Driver: <code>'+esc(e.state)+'</code>':'')+' · <a href="'+esc(e.url)+'" target="_blank" rel="noreferrer">Original PNG</a>';el('badge').textContent=badge(e);el('badge').className='pill '+(e.raw?.pixels===0?'green':e.status==='captured'?'amber':'muted');const note=[e.note,e.error].filter(Boolean).join(' ');el('note').textContent=note;el('note').classList.toggle('hidden',!note);
el('metrics').innerHTML=e.raw?[['Full image',fmt(e.raw.pixels)+' changed',pct(e.raw.pixels,e.raw.compared)+' of '+fmt(e.raw.compared)+' pixels'],['Outside excluded regions',fmt(e.adjusted.pixels)+' changed',pct(e.adjusted.pixels,e.adjusted.compared)+' of '+fmt(e.adjusted.compared)+' pixels'],['Excluded area',fmt(e.maskedPixels??0)+' pixels',pct(e.maskedPixels??0,e.raw.compared)+' of the full image']].map(([label,value,detail])=>'<div class="metric"><span>'+label+'</span><strong>'+value+'</strong><span>'+detail+'</span></div>').join(''):'';
const hasActual=!!e.images?.actual,hasRef=!!e.images?.reference;el('single').classList.toggle('hidden',view==='side'||!hasRef);el('side').classList.toggle('hidden',view!=='side'||!hasRef);el('empty').classList.toggle('hidden',hasActual);el('empty').textContent=hasActual?'':'No equivalent current capture is available for this gallery screenshot. '+(e.note??'')+(e.error?' Capture error: '+e.error:'');
el('base').src=view==='diff'&&hasActual?(el('adjusted').checked?e.images.maskedDiff:e.images.rawDiff):e.images?.reference??'';el('current').src=e.images?.actual??'';el('current').classList.toggle('hidden',view!=='overlay'||!hasActual);el('wipe-line').classList.toggle('hidden',view!=='overlay'||!hasActual);el('range-wrap').classList.toggle('hidden',view!=='overlay'||!hasActual);el('side-ref').src=e.images?.reference??'';el('side-current').src=e.images?.actual??'';el('side-current').classList.toggle('hidden',!hasActual);el('adjusted-label').classList.toggle('hidden',view!=='diff');el('left-label').textContent=view==='diff'?'Pink = changed pixels':hasActual?'Current app (left of wipe)':'Gallery reference';el('right-label').textContent=view==='diff'?(el('adjusted').checked?'Excluded pixels omitted':'No masks; no tolerance'):'Gallery reference (right of wipe)';
el('masks').innerHTML=el('show-masks').checked?(e.masks??[]).map(m=>'<div class="mask" style="left:'+(m.x/80*100)+'%;top:'+(m.y/25*100)+'%;width:'+(m.w/80*100)+'%;height:'+(m.h/25*100)+'%"></div>').join(''):'';
const maskRows=(e.masks??[]).map(m=>'<li><code>x='+m.x+', y='+m.y+', width='+m.w+', height='+m.h+'</code> cells: '+esc(m.reason)+'</li>').join('');const cells=(e.raw?.cells??[]).slice(0,10).map(c=>'<tr><td>'+c.x+', '+c.y+'</td><td>'+c.x*9+', '+c.y*16+'</td><td>'+fmt(c.pixels)+'</td></tr>').join('');
el('method').innerHTML='<p>Exact RGBA comparison at native 720 × 400 resolution, with zero color tolerance and no image registration, resizing, or antialias allowance. Each character cell is 9 × 16 pixels. Animation is disabled; the app caret is frozen in its visible state. Every subject starts in a new browser context, with empty browser storage. Reference PNGs are separate from updateable test snapshots.</p><p>The main metric compares every pixel, including text, title, status, and output. The adjusted metric excludes the full rectangles listed below. Partial subject mappings may show a current fallback or incomplete help topic; a low pixel count does not prove equivalent functionality.</p>'+(maskRows?'<ul>'+maskRows+'</ul>':'<p>No raster exclusions for this subject.</p>')+'<p>Legacy cell-attribute mismatches: '+(e.cellMismatches??'not measured')+'. This is a secondary layout diagnostic and does not establish glyph or text equality.</p>'+(cells?'<details><summary>Top changed cells in the unmasked image</summary><table><tr><th>Cell x,y</th><th>Pixel x,y</th><th>Changed pixels</th></tr>'+cells+'</table></details>':'')+'<p>Captured: '+esc(e.capturedAt??'not captured')+'<br>Reference SHA-256: <code>'+esc(e.referenceHash??'missing')+'</code><br>Current PNG SHA-256: <code>'+esc(e.actualHash??'not captured')+'</code><br>Source tree SHA-256: <code>'+esc(e.sourceHash??report.sourceHashStart)+'</code><br>Driver SHA-256: <code>'+(e.captureDriverHash??report.driverHash)+'</code></p>';
}
el('search').oninput=list;el('filter').onchange=list;el('show-masks').onchange=render;el('adjusted').onchange=render;
el('wipe').oninput=()=>{const n=Number(el('wipe').value);el('current').style.clipPath='inset(0 '+(100-n)+'% 0 0)';el('wipe-line').style.left=n+'%'};
document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>{view=b.dataset.view;render()});
el('previous').onclick=()=>{active=(active+entries.length-1)%entries.length;render()};el('next').onclick=()=>{active=(active+1)%entries.length;render()};render();
</script></body></html>`;
}
